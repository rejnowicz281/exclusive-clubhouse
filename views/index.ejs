<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/head.ejs") %>
    <body>
        <%- include("./partials/nav.ejs") %>
        <h1>Home</h1>
        <% if(currentUser) {%>
        <p>Welcome, <%= currentUser.identifier %></p>
        <% if(currentUser.is_admin) {%>
        <p>You are an <b>admin</b>.</p>
        <% } %> <% if(currentUser.is_member) {%>
        <p>You are a <b>member</b>.</p>
        <% } %> <% } else{ %>
        <p>Welcome, guest.</p>
        <% } %>
        <a href="/posts">Go to posts</a>
    </body>
</html>
