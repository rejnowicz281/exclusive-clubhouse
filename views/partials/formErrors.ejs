<% if(locals.errors || locals.messages.error) {%>
<ul>
    <% if(messages.error) {%>
    <li><%= messages.error %></li>
    <% } %> <% if(locals.errors) {%> <% errors.forEach(error => { %>
    <li><%= error.msg %></li>
    <% }) %> <% } %>
</ul>
<% } %>
